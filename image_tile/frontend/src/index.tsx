import React from "react"
import ReactDOM from "react-dom"
import CaptionedImage from "./ImageTile"

ReactDOM.render(
  <React.StrictMode>
    <CaptionedImage />
  </React.StrictMode>,
  document.getElementById("root")
)
